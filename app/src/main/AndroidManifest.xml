<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Permisos necesarios para la ubicación y Google Maps -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" /> <!-- Permiso para obtener la ubicación precisa del usuario -->
    <uses-permission android:name="android.permission.ACCESS_BACKGROUND_LOCATION" />



    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PooFinal">
        <activity
            android:name=".verificarPin"
            android:exported="false" />
        <activity
            android:name=".ConfigurarPin"
            android:exported="false" /> <!-- Declaración de actividades -->
        <activity
            android:name=".shareContactosView"
            android:exported="false" />
        <activity
            android:name=".borrarDataView"
            android:exported="false" />
        <activity
            android:name=".appsConfiguracionView"
            android:exported="false" />
        <activity
            android:name=".confiCodeView"
            android:exported="false" />
        <activity
            android:name=".CodigoView"
            android:exported="false" />
        <activity
            android:name=".MenuView"
            android:exported="false" /> <!-- MainActivity como la actividad de inicio de la app -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>